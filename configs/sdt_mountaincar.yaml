env: MountainCar-v0
dataset: data/mountaincar_random.pkl
seq_len: 20
batch_size: 64
hidden_dim: 128
n_layers: 4
learning_rate: 1e-4
epochs: 1000
eval_every: 10
checkpoint_every: 50
seed: 42
num_workers: 1

snn:
  lif_tau: 20.0
  surrogate_k: 25.0
  v_th: 0.3
  current_scale: 1.0